<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.kaist.board">

    <!--  -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" />

    <!--  -->
    <application
        android:name=".utils.Application"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Board">
        <activity
            android:name=".ui.SplashActivity"
            android:icon="@drawable/ic_launcher"
            android:theme="@android:style/Theme.Holo.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:icon="@drawable/ic_launcher_reverse"
            android:label="" />
        <activity
            android:name=".ui.SettingDeveloperActivity"
            android:icon="@drawable/ic_launcher_reverse"
            android:label="만든 사람들" />
        <activity
            android:name=".ui.DetailActivity"
            android:icon="@drawable/ic_launcher_reverse"
            android:label="" />
        <activity
            android:name=".ui.ImageActivity"
            android:icon="@drawable/ic_launcher_reverse"
            android:label=""
            android:theme="@style/Theme.Board.Black"></activity>
    </application>

</manifest>
